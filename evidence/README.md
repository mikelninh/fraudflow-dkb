# Evidence

This directory contains reproducible proof generated by tests/evals, not hand-written success claims.

`eval-results.json` is produced by:

```bash
python evals/run_evals.py --check
```

The committed result is a snapshot for reviewer inspection. CI executes the same suite again on every push and pull request.
